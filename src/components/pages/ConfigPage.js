import { Layout } from 'antd';
import '../../styles/components/pages/ConfigPage.scss';

const { Content } = Layout;

const ConfigPage = () => {

  return (
    <Content className='config-page'> Config Page </Content>
  );
};

export default ConfigPage;
