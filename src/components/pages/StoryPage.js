import { Layout } from 'antd';
import '../../styles/components/pages/StoryPage.scss';

const { Content } = Layout;

const StoryPage = () => {

  return (
    <Content className='story-page'> Story Page </Content>
  );
};

export default StoryPage;
